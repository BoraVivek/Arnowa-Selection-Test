<header class="bg-slate-500 h-16  flex justify-center shadow-lg border-b-4 border-white">
    <div class="w-10/12 flex justify-between items-center">
        <h2 class="text-4xl font-bold text-white font-mono">
            <a href="/">Arnowa</a>
        </h2>
        <nav>
            <ul class="flex gap-8 text-white">
                <li class="<%= active == "home" ? "activeMenu" : '' %>">
                    <a href="/">Home</a>
                </li>
                <li class="<%= active == "login" ? "activeMenu" : '' %></li>">
                <a href="/login">Login</a>
                </li>
                <li class="<%= active == "register" ? "activeMenu" : '' %></li>">
                <a href="/register">Register</a>
                </li>
            </ul>
        </nav>
    </div>
</header>